import 'package:flutter/material.dart';
/*
class InstallationStateIndicator extends StatelessWidget {
  //final InstallationState state;
  final double height;
  final double width;
  const InstallationStateIndicator(this.state, {this.height = 100, this.width = 200, super.key});
  
  @override
  Widget build(BuildContext context) {

    return Container(
        color: Colors.transparent,//state.colour,
        padding: const EdgeInsets.symmetric(horizontal: 50.0),
        height: height,
        width: width,
        child: Center(
        child: Text(state.text,
          maxLines: 2,
          style: Theme.of(context).textTheme.titleMedium?.copyWith(
            color: state.colour,
            fontWeight: FontWeight.w200,
          )
        ))
    );
  }
}*/